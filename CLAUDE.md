# lstep-portal

「創業2年で売上3億・組織100人を実現する仕組み化研修」の会員専用ポータルサイト。
株式会社MARKELINE（代表：藤原一輝）が運営する研修プログラムの経営者メンバー向け管理画面。

## 構成

単一ファイルの静的サイト（`index.html`、ビルド不要）。SPA的にJSでセクションを切り替える構成で、
社内PDF資料を base64 data URI として埋め込んでいるため 2000行超・数MBのファイルサイズになっている。

`index.html` 内のセクション（`.page-section` / `id="page-*"`）:
- `page-dashboard` — ダッシュボード（トップ）
- `page-documents` — 配布資料（PDF）
- `page-schedule` — スケジュール
- `page-homework` — 課題・宿題
- `page-qna` — Q&A（`QNA` という JS の配列で管理。橋本氏が回答者）
- `page-members` — メンバー一覧
- `page-kennshu` — 研修コンテンツ
- `page-jigyo` — 事業計画（`@media print` で単体印刷対応済み、`html2pdf.js` を読み込み済み）

## 既知の問題

- **ログインゲートが機能していなかった問題は2026-09-01に修正済み**: `tryLogin()` を実装し、起動時に
  無条件で `login-screen` を非表示にしていたバグを削除した。単一の共通PIN（`PORTAL_PIN` 定数、
  `index.html` の `<script>` 末尾付近）でゲートする方式。現在のPINは藤原さんの指示で `2001`。
  クライアントサイドの平文比較なので view-source で読めば誰でも突破できる程度の抑止力しかない
  （本気の会員限定制御が必要になったら、サーバーサイド認証への移行を検討すること）。
  Playwrightでの動作確認済み（誤PIN→拒否、正PIN→ポータル表示）。
- Git履歴は "Add files via upload" の1コミットのみ（GitHub Web UIから手動アップロードされたもの）。
  私（Claude）とのペア開発ではまだ育っていないファイルなので、既存の命名規則・書き方の癖を壊さないよう
  差分は最小限にすること。

## 関連コンテキスト（別リポジトリ・別セッション）

株式会社MARKELINEでは `MARKELINE-Inc-lstep/keiri-form` リポジトリ側で、Notion上に非常に発達した
「AI秘書室」「AI組織運行ボード（ミス京子＝PM／京子CS／京子グロース）」という自律運用の仕組みが
別セッションによってすでに稼働中（日次巡回・週次報告・エスカレーション等）。

2026-09-01、藤原さんの指示によりこのリポジトリ（lstep-portal）を「案件DB」
（`Lステップ/AI業務効率化 PMエンジン` 配下）に登録済み
（クライアント名「仕組み化研修ポータル（社内・lstep-portal）」）。
以後はAI組織運行ボード側からも進捗が追跡される想定。このリポジトリで作業した際は、
案件DBの該当行（ステータス・現在のタスク・進捗判定等）も合わせて更新すると
運行ボード側との二重管理・情報の食い違いを防げる。
