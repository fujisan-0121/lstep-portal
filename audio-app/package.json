{
  "name": "markeline-audio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:full": "node scripts/deploy.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node --test test/",
    "db:migrate:local": "wrangler d1 migrations apply audio_db --local",
    "db:migrate": "wrangler d1 migrations apply audio_db --remote"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260908.1",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
